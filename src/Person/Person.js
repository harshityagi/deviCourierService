import React from 'react';

const person = (props) => {
	return (
        <div>
        <p>{props.name} is very hard working at {props.age} years of age!!</p>
        </div>
    )    
    };

export default person;